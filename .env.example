# ==============================================================================
# EU5 Standalone Agent Configuration
# ==============================================================================
# Copy this file to .env and fill in your values
# Get your API key from: https://platform.openai.com/api-keys

# OpenAI API Key (REQUIRED)
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Model Selection (optional)
# Default: gpt-5-mini
# Available models:
#   - gpt-5-mini (recommended: fast, cost-effective, good quality)
#   - gpt-5 (more capable but expensive)
#   - gpt-4o (previous generation, still good)
#   - gpt-4-turbo (previous generation)
OPENAI_MODEL=gpt-5-mini

# Important Notes for gpt-5-mini:
# - Does NOT support temperature parameter (uses default=1.0)
# - Uses max_completion_tokens instead of max_tokens
# - The agent handles these differences automatically

# OpenAI API Base URL (optional)
# Default: https://api.openai.com/v1
# Only change if using a proxy or alternative endpoint
# OPENAI_BASE_URL=https://api.openai.com/v1

# Knowledge Base Path (optional)
# Path to directory containing EU5 knowledge base markdown files
# Default: Repository's 'knowledge/' directory
# Only set this if you want to use a custom knowledge base location
# EU5_KNOWLEDGE_PATH=/path/to/custom/knowledge

# ==============================================================================
# Web Search Configuration (Optional)
# ==============================================================================
# Tavily API provides AI-optimized web search when knowledge base is insufficient
# Get FREE API key (1000 searches/month): https://tavily.com/
# This is OPTIONAL - agent works fine without it using local knowledge base only

# TAVILY_API_KEY=your-tavily-api-key-here

# Why use Tavily:
#  - Enables web search for content not in local knowledge base
#  - AI-optimized search results (clean text, no HTML)
#  - Domain filtering (prioritizes eu5.paradoxwikis.com)
#  - 1000 free searches per month
#
# Without Tavily:
#  - Agent still works perfectly for all content in knowledge base
#  - Queries outside knowledge base will gracefully explain no web search available

# ==============================================================================
# Conversation History Limit (Optional)
# ==============================================================================
# Maximum number of messages kept in conversation history.
# Older turn groups are trimmed to prevent exceeding the model's context window.
# Default: 100
# EU5_MAX_HISTORY_MESSAGES=100

# ==============================================================================
# Usage Examples
# ==============================================================================
# After setting up your .env file:
#
# Interactive mode:
#   python run_agent.py
#
# Single query:
#   python run_agent.py --query "How do estates work?"
#
# With verbose output:
#   python run_agent.py --query "England strategy" --verbose
#
# You can also set these as environment variables instead of using .env:
#   export OPENAI_API_KEY='your-key-here'
#   export OPENAI_MODEL='gpt-5-mini'
#   python run_agent.py
