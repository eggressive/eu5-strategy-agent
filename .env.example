# ==============================================================================
# EU5 Standalone Agent Configuration
# ==============================================================================
# Copy this file to .env and fill in your values
# Get your API key from: https://platform.openai.com/api-keys

# OpenAI API Key (REQUIRED)
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Model Selection (optional)
# Default: gpt-5-mini
# Available models:
#   - gpt-5-mini (recommended: fast, cost-effective, good quality)
#   - gpt-5 (more capable but expensive)
#   - gpt-4o (previous generation, still good)
#   - gpt-4-turbo (previous generation)
OPENAI_MODEL=gpt-5-mini

# Important Notes for gpt-5-mini:
# - Does NOT support temperature parameter (uses default=1.0)
# - Uses max_completion_tokens instead of max_tokens
# - The agent handles these differences automatically

# OpenAI API Base URL (optional)
# Default: https://api.openai.com/v1
# Only change if using a proxy or alternative endpoint
# OPENAI_BASE_URL=https://api.openai.com/v1

# Knowledge Base Path (optional)
# Path to directory containing EU5 knowledge base markdown files
# Default: /home/dimitar/ai/eu5_agent
# Must contain files like: economy_mechanics.md, society_mechanics.md, etc.
EU5_KNOWLEDGE_PATH=/home/dimitar/ai/eu5_agent

# ==============================================================================
# Usage Examples
# ==============================================================================
# After setting up your .env file:
#
# Interactive mode:
#   python run_eu5_standalone.py
#
# Single query:
#   python run_eu5_standalone.py --query "How do estates work?"
#
# With verbose output:
#   python run_eu5_standalone.py --query "England strategy" --verbose
#
# You can also set these as environment variables instead of using .env:
#   export OPENAI_API_KEY='your-key-here'
#   export OPENAI_MODEL='gpt-5-mini'
#   python run_eu5_standalone.py
